# i18n (Tiếng Việt / Tiếng Anh) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Thêm hỗ trợ đa ngôn ngữ VI/EN cho toàn bộ frontend Concord với toggle `VI | EN` ở header, lưu preference vào cookie `NEXT_LOCALE`.

**Architecture:** Dùng `next-intl` v4, không URL prefix — URL giữ nguyên, locale resolve qua cookie. Server + client components đều dùng `useTranslations('namespace')` (tất cả pages hiện tại là `'use client'`). Toggle ở header ghi cookie rồi `router.refresh()`.

**Tech Stack:** next-intl v4, Next.js 16 App Router, TypeScript, cookie-based locale (không localStorage).

---

## File Map

**Tạo mới:**
- `apps/web/messages/vi.json` — toàn bộ strings tiếng Việt
- `apps/web/messages/en.json` — toàn bộ strings tiếng Anh
- `apps/web/i18n/routing.ts` — locales + defaultLocale
- `apps/web/i18n/request.ts` — next-intl plugin config
- `apps/web/middleware.ts` — đọc cookie NEXT_LOCALE
- `apps/web/global.d.ts` — IntlMessages type declaration
- `apps/web/components/layout/locale-toggle.tsx` — VI/EN toggle component

**Sửa:**
- `apps/web/next.config.ts` — wire next-intl plugin
- `apps/web/tsconfig.json` — thêm path alias cho i18n/routing
- `apps/web/components/layout/header.tsx` — thêm LocaleToggle
- `apps/web/components/layout/sidebar.tsx` — dùng `useTranslations('nav')`
- `apps/web/components/ui/index.tsx` — dùng `useTranslations('common')` cho PageHeader, EmptyState
- `apps/web/app/login/page.tsx`
- `apps/web/app/register/page.tsx` (nếu tồn tại)
- `apps/web/app/invite/[token]/page.tsx`
- `apps/web/features/auth/components/change-password-modal.tsx`
- `apps/web/app/(authed)/settings/page.tsx`
- `apps/web/app/(authed)/finance-settings/page.tsx`
- `apps/web/app/(authed)/transactions/page.tsx`
- `apps/web/features/transactions/components/edit-transaction-modal.tsx`
- `apps/web/features/transactions/components/day-group.tsx`
- `apps/web/app/(authed)/goals/page.tsx`
- `apps/web/app/(authed)/reports/page.tsx`
- `apps/web/app/(authed)/weekly/page.tsx`
- `apps/web/features/tasks/components/task-board.tsx`
- `apps/web/features/tasks/components/task-card.tsx`
- `apps/web/features/tasks/components/task-quick-add.tsx`
- `apps/web/app/(authed)/important-dates/page.tsx`
- `apps/web/app/(authed)/important-dates/calendar/page.tsx`
- `apps/web/app/(authed)/important-dates/year/page.tsx`
- `apps/web/features/important-dates/components/` (các components)
- `apps/web/app/(authed)/chat/page.tsx`
- `apps/web/app/(authed)/dashboard/page.tsx`
- `apps/web/app/(authed)/family/setup/page.tsx`
- `apps/web/app/(authed)/family/invite/page.tsx`

---

## Task 1: Install next-intl và setup infrastructure

**Files:**
- Modify: `apps/web/package.json` (via pnpm)
- Create: `apps/web/i18n/routing.ts`
- Create: `apps/web/i18n/request.ts`
- Create: `apps/web/middleware.ts`
- Modify: `apps/web/next.config.ts`
- Modify: `apps/web/tsconfig.json`
- Create: `apps/web/global.d.ts`

- [ ] **Step 1: Install next-intl**

```bash
cd /path/to/concord
pnpm --filter web add next-intl
```

Expected: next-intl xuất hiện trong `apps/web/package.json` dependencies.

- [ ] **Step 2: Tạo `apps/web/i18n/routing.ts`**

```ts
import { defineRouting } from 'next-intl/routing';

export const routing = defineRouting({
  locales: ['vi', 'en'],
  defaultLocale: 'vi',
  localePrefix: 'never',
});
```

- [ ] **Step 3: Tạo `apps/web/i18n/request.ts`**

```ts
import { getRequestConfig } from 'next-intl/server';
import { routing } from './routing';

export default getRequestConfig(async ({ requestLocale }) => {
  let locale = await requestLocale;
  if (!locale || !routing.locales.includes(locale as 'vi' | 'en')) {
    locale = routing.defaultLocale;
  }
  return {
    locale,
    messages: (await import(`../messages/${locale}.json`)).default,
  };
});
```

- [ ] **Step 4: Tạo `apps/web/middleware.ts`**

```ts
import createMiddleware from 'next-intl/middleware';
import { routing } from './i18n/routing';

export default createMiddleware(routing);

export const config = {
  matcher: ['/((?!_next|api|.*\\..*).*)'],
};
```

- [ ] **Step 5: Sửa `apps/web/next.config.ts`**

```ts
import type { NextConfig } from 'next';
import createNextIntlPlugin from 'next-intl/plugin';

const withNextIntl = createNextIntlPlugin('./i18n/request.ts');

const nextConfig: NextConfig = {};

export default withNextIntl(nextConfig);
```

- [ ] **Step 6: Sửa `apps/web/tsconfig.json` — thêm path alias**

Trong `compilerOptions.paths`, thêm dòng sau (giữ nguyên `"@/*": ["./*"]`):

```json
"paths": {
  "@/*": ["./*"],
  "@/i18n/routing": ["./i18n/routing.ts"]
}
```

- [ ] **Step 7: Tạo `apps/web/global.d.ts`**

```ts
import vi from './messages/vi.json';
type Messages = typeof vi;
declare global {
  interface IntlMessages extends Messages {}
}
```

- [ ] **Step 8: Verify build không lỗi**

```bash
pnpm --filter web build
```

Expected: build thành công (có thể có warning về missing messages files — sẽ fix ở Task 2).

- [ ] **Step 9: Commit**

```bash
git add apps/web/i18n/ apps/web/middleware.ts apps/web/next.config.ts apps/web/tsconfig.json apps/web/global.d.ts apps/web/package.json pnpm-lock.yaml
git commit -m "feat(web): setup next-intl infrastructure (routing, middleware, config)"
```

---

## Task 2: Tạo message files vi.json và en.json

**Files:**
- Create: `apps/web/messages/vi.json`
- Create: `apps/web/messages/en.json`

- [ ] **Step 1: Tạo `apps/web/messages/vi.json`**

```json
{
  "common": {
    "save": "Lưu",
    "saving": "Đang lưu…",
    "cancel": "Huỷ",
    "reset": "Reset",
    "close": "Đóng",
    "edit": "Chỉnh sửa",
    "delete": "Xoá",
    "loading": "Đang tải…",
    "error": "Lỗi không xác định",
    "soon": "Soon",
    "logout": "Đăng xuất",
    "confirm_delete_transaction": "Xoá giao dịch này? Số dư quỹ sẽ được hoàn lại.",
    "cannot_delete": "Không xoá được: {msg}"
  },
  "nav": {
    "overview": "Tổng quan",
    "ai_assistant": "Trợ lý AI",
    "finance": "Tài chính",
    "transactions": "Giao dịch",
    "reports": "Báo cáo",
    "savings_investment": "Tiết kiệm & Đầu tư",
    "settings": "Cài đặt",
    "family_group": "Gia đình",
    "tasks": "Công việc",
    "anniversaries": "Ngày kỷ niệm",
    "members": "Thành viên",
    "your_funds": "Quỹ của bạn",
    "profile": "Profile"
  },
  "auth": {
    "login_title": "Đăng nhập",
    "login_subtitle": "Nhập email và mật khẩu để tiếp tục.",
    "login_tagline": "Cùng vợ chồng đi tới mục tiêu tài chính chung.",
    "submitting": "Đang đăng nhập…",
    "no_account": "Chưa có tài khoản?",
    "register_link": "Đăng ký",
    "register_title": "Tạo tài khoản",
    "already_account": "Đã có tài khoản?",
    "login_link": "Đăng nhập",
    "email": "Email",
    "password": "Mật khẩu",
    "confirm_password": "Xác nhận mật khẩu",
    "name": "Tên",
    "role": "Vai trò",
    "husband": "Chồng",
    "wife": "Vợ",
    "change_password": "Đổi mật khẩu",
    "current_password": "Mật khẩu hiện tại",
    "new_password": "Mật khẩu mới",
    "birthdate": "Ngày sinh",
    "change_password_success": "Đổi mật khẩu thành công"
  },
  "settings": {
    "title": "Cài đặt",
    "subtitle": "Tài khoản và mật khẩu",
    "account": "Tài khoản",
    "couple_only_note": "Concord là couple-only — mỗi instance chỉ có 2 tài khoản (vợ + chồng).",
    "click_to_change_avatar": "Bấm vào ảnh để thay đổi",
    "user_id": "User ID",
    "role_label": "Vai trò",
    "email_label": "Email",
    "id_label": "ID",
    "birthdate_label": "Ngày sinh",
    "click_edit": "✏️ Chỉnh sửa"
  },
  "finance": {
    "title": "Cài đặt tài chính",
    "subtitle": "Mục tiêu tiết kiệm năm và số dư khởi đầu các quỹ.",
    "yearly_goal_title": "Mục tiêu tiết kiệm năm {year}",
    "yearly_goal_desc": "Số tiền 2 vợ chồng muốn để dành cùng nhau trong năm. Tiến độ tính bằng tổng tiền đã chuyển vào các quỹ tiết kiệm & đầu tư trong năm — không phải thu/chi của quỹ chi tiêu.",
    "goal_vnd": "Mục tiêu (VND)",
    "goal_placeholder": "vd: 150000000 (150 triệu)",
    "goal_hint": "Nhập số nguyên VND, vd: 150000000.",
    "goal_preview": "Hiển thị:",
    "goal_saved": "Đã lưu mục tiêu",
    "opening_balance_title": "Số dư khởi đầu",
    "opening_balance_desc": "Tiền đã có sẵn trong mỗi quỹ tại thời điểm bạn bắt đầu xài Concord (vd: số dư app ngân hàng, ví Shopee, tiền mặt). Khoản này được ghi như 1 entry cấu trúc — không hiện trong \"Giao dịch gần đây\" và không tính vào báo cáo thu/chi tháng. Số dư khởi đầu của quỹ tiết kiệm & đầu tư không tính vào tiến độ tiết kiệm năm.",
    "current_balance": "Số dư hiện tại",
    "balance_saved": "Đã lưu"
  },
  "transactions": {
    "title": "Giao dịch",
    "subtitle": "Lịch sử thu chi theo quỹ.",
    "no_transactions": "Chưa có giao dịch nào",
    "no_transactions_desc": "Vào Chat để ghi giao dịch bằng ngôn ngữ tự nhiên.",
    "add_transaction": "Thêm giao dịch",
    "amount": "Số tiền (VND)",
    "category": "Danh mục",
    "note": "Ghi chú",
    "fund": "Quỹ",
    "type": "Loại",
    "income": "Thu nhập",
    "expense": "Chi tiêu",
    "transfer": "Chuyển nội bộ",
    "edit_title": "Sửa giao dịch",
    "delete_confirm": "Xoá giao dịch này? Số dư quỹ sẽ được hoàn lại.",
    "all_funds": "Tất cả quỹ",
    "prev_month": "Tháng trước",
    "next_month": "Tháng sau",
    "page": "Trang {page}",
    "no_category": "(không ghi chú)"
  },
  "goals": {
    "title": "Quỹ tiết kiệm & đầu tư",
    "subtitle": "Tích luỹ theo mục tiêu cụ thể. Chuyển tiền từ quỹ chi tiêu vào đây để bắt đầu tiết kiệm.",
    "yearly_progress": "Tiến độ tiết kiệm năm",
    "no_goals": "Chưa có quỹ tiết kiệm nào",
    "no_goals_desc": "Tạo quỹ đầu tiên để bắt đầu tiết kiệm.",
    "create_fund": "+ Tạo quỹ",
    "ahead": "Đang vượt tiến độ",
    "on_track": "Đúng tiến độ",
    "behind": "Đang chậm",
    "reached": "Đạt",
    "overdue": "Chậm",
    "ahead_short": "Vượt",
    "days_remaining": "Còn {days} ngày",
    "month_contribution": "Tháng này: {current} / {target}",
    "manage": "Quản lý →"
  },
  "reports": {
    "title": "Báo cáo",
    "subtitle": "Phân tích thu chi theo tháng.",
    "income": "Tháng này — thu",
    "expense": "Tháng này — chi",
    "net": "Tháng này — net",
    "txn_count": "{count} giao dịch tổng",
    "categories_count": "{count} mục có chi tiêu",
    "net_hint": "Thu trừ chi",
    "no_data": "Không có dữ liệu",
    "no_data_desc": "Chưa có giao dịch nào trong tháng này.",
    "expense_by_category": "Chi tiêu theo danh mục",
    "current_month": "Tháng hiện tại"
  },
  "tasks": {
    "title": "Công việc tuần",
    "subtitle": "Quản lý công việc gia đình theo tuần.",
    "add_task": "Thêm việc",
    "no_tasks": "Chưa có việc nào",
    "no_tasks_col": "Chưa có việc",
    "note_placeholder": "Ghi chú…",
    "status_todo": "Cần làm",
    "status_in_progress": "Đang làm",
    "status_done": "Xong",
    "start": "Bắt đầu",
    "done": "Xong",
    "completed": "Hoàn thành",
    "category_shopping": "Mua sắm",
    "category_chores": "Việc nhà",
    "category_finance": "Tài chính",
    "category_goal": "Mục tiêu",
    "assignee_label": "Giao cho",
    "assignee_husband": "Chồng",
    "assignee_wife": "Vợ",
    "assignee_both": "Cả hai",
    "delete_label": "Xóa",
    "title_placeholder": "Tên công việc…",
    "add_button": "+ Thêm"
  },
  "dates": {
    "title": "Ngày kỷ niệm",
    "subtitle": "Lưu lại những ngày đặc biệt.",
    "add_date": "+ Thêm ngày",
    "no_dates": "Chưa có ngày kỷ niệm nào",
    "no_dates_desc": "Thêm ngày đặc biệt để nhận nhắc nhở.",
    "upcoming": "Sắp tới",
    "past": "Đã qua",
    "view_calendar": "Lịch",
    "view_list": "Danh sách",
    "view_year": "Năm",
    "days_until": "còn {days} ngày",
    "days_ago": "{days} ngày trước",
    "today": "Hôm nay",
    "tomorrow": "Ngày mai",
    "edit": "Sửa",
    "delete": "Xoá"
  },
  "chat": {
    "title": "Trợ lý AI",
    "subtitle": "Chat với Concord AI về tài chính gia đình.",
    "placeholder": "Nhập tin nhắn…",
    "send": "Gửi",
    "thinking": "Đang suy nghĩ…",
    "empty_title": "Xin chào!",
    "empty_desc": "Hãy bắt đầu bằng cách nhập một giao dịch, ví dụ: \"Sáng nay mua cà phê 45k\"."
  },
  "dashboard": {
    "title": "Chào {name} 👋",
    "invite_title": "Mời vợ/chồng tham gia gia đình",
    "invite_desc": "Sau khi 2 người cùng tham gia, hệ thống sẽ tự tạo 3 quỹ chi tiêu (Chồng / Vợ / Chung) để bắt đầu.",
    "invite_cta": "Gửi link mời ngay →",
    "year_goal": "Mục tiêu năm {year}",
    "year_goal_desc": "Tiết kiệm {amount} cùng nhau",
    "remaining": "Còn lại",
    "days_remaining": "Còn ngày",
    "days_remaining_value": "{days} ngày",
    "projection": "Dự kiến cuối kỳ",
    "goal_reached": "Đạt mục tiêu",
    "goal_missed": "Hụt mục tiêu",
    "goal_progress_hint": "Tính theo dòng tiền vào quỹ tiết kiệm & đầu tư trong năm",
    "funds_balance": "Số dư quỹ",
    "total_visible": "Tổng (bạn thấy được): {amount}",
    "savings_investment": "Tiết kiệm & đầu tư",
    "recent_transactions": "Giao dịch gần đây",
    "add_via_chat": "+ Thêm qua chat",
    "no_recent": "Chưa có giao dịch nào",
    "no_recent_desc": "Vào tab Chat để gõ giao dịch tự nhiên — agent sẽ log vào quỹ tương ứng.",
    "target_funds": "Quỹ mục tiêu"
  },
  "family": {
    "invite_title": "Mời thành viên",
    "invite_subtitle": "Gửi link mời để vợ/chồng tham gia.",
    "setup_title": "Thiết lập gia đình",
    "setup_subtitle": "Tạo gia đình để bắt đầu quản lý tài chính cùng nhau."
  }
}
```

- [ ] **Step 2: Tạo `apps/web/messages/en.json`**

```json
{
  "common": {
    "save": "Save",
    "saving": "Saving…",
    "cancel": "Cancel",
    "reset": "Reset",
    "close": "Close",
    "edit": "Edit",
    "delete": "Delete",
    "loading": "Loading…",
    "error": "Unknown error",
    "soon": "Soon",
    "logout": "Log out",
    "confirm_delete_transaction": "Delete this transaction? The fund balance will be restored.",
    "cannot_delete": "Cannot delete: {msg}"
  },
  "nav": {
    "overview": "Overview",
    "ai_assistant": "AI Assistant",
    "finance": "Finance",
    "transactions": "Transactions",
    "reports": "Reports",
    "savings_investment": "Savings & Investment",
    "settings": "Settings",
    "family_group": "Family",
    "tasks": "Tasks",
    "anniversaries": "Anniversaries",
    "members": "Members",
    "your_funds": "Your Funds",
    "profile": "Profile"
  },
  "auth": {
    "login_title": "Sign in",
    "login_subtitle": "Enter your email and password to continue.",
    "login_tagline": "Reach your financial goals together.",
    "submitting": "Signing in…",
    "no_account": "Don't have an account?",
    "register_link": "Sign up",
    "register_title": "Create account",
    "already_account": "Already have an account?",
    "login_link": "Sign in",
    "email": "Email",
    "password": "Password",
    "confirm_password": "Confirm password",
    "name": "Name",
    "role": "Role",
    "husband": "Husband",
    "wife": "Wife",
    "change_password": "Change password",
    "current_password": "Current password",
    "new_password": "New password",
    "birthdate": "Date of birth",
    "change_password_success": "Password changed successfully"
  },
  "settings": {
    "title": "Settings",
    "subtitle": "Account and password",
    "account": "Account",
    "couple_only_note": "Concord is couple-only — each instance has exactly 2 accounts (husband + wife).",
    "click_to_change_avatar": "Click photo to change",
    "user_id": "User ID",
    "role_label": "Role",
    "email_label": "Email",
    "id_label": "ID",
    "birthdate_label": "Date of birth",
    "click_edit": "✏️ Edit"
  },
  "finance": {
    "title": "Finance Settings",
    "subtitle": "Annual savings goal and fund opening balances.",
    "yearly_goal_title": "Annual savings goal {year}",
    "yearly_goal_desc": "The amount you and your partner want to save together this year. Progress is measured by the total transferred into savings & investment funds — not income/expense of the spending fund.",
    "goal_vnd": "Goal (VND)",
    "goal_placeholder": "e.g. 150000000 (150 million)",
    "goal_hint": "Enter integer VND, e.g. 150000000.",
    "goal_preview": "Preview:",
    "goal_saved": "Goal saved",
    "opening_balance_title": "Opening Balance",
    "opening_balance_desc": "Money already in each fund when you started using Concord (e.g. bank balance, e-wallets, cash). This is recorded as a structural entry — it does not appear in \"Recent Transactions\" and is not counted in monthly income/expense reports. Opening balance of savings & investment funds does not count toward the annual savings goal.",
    "current_balance": "Current balance",
    "balance_saved": "Saved"
  },
  "transactions": {
    "title": "Transactions",
    "subtitle": "Income and expense history by fund.",
    "no_transactions": "No transactions yet",
    "no_transactions_desc": "Go to Chat to log transactions in natural language.",
    "add_transaction": "Add transaction",
    "amount": "Amount (VND)",
    "category": "Category",
    "note": "Note",
    "fund": "Fund",
    "type": "Type",
    "income": "Income",
    "expense": "Expense",
    "transfer": "Internal transfer",
    "edit_title": "Edit transaction",
    "delete_confirm": "Delete this transaction? The fund balance will be restored.",
    "all_funds": "All funds",
    "prev_month": "Previous month",
    "next_month": "Next month",
    "page": "Page {page}",
    "no_category": "(no note)"
  },
  "goals": {
    "title": "Savings & Investment Funds",
    "subtitle": "Save toward specific goals. Transfer from spending funds to start saving.",
    "yearly_progress": "Annual savings progress",
    "no_goals": "No savings funds yet",
    "no_goals_desc": "Create your first fund to start saving.",
    "create_fund": "+ Create fund",
    "ahead": "Ahead of schedule",
    "on_track": "On track",
    "behind": "Behind schedule",
    "reached": "Reached",
    "overdue": "Behind",
    "ahead_short": "Ahead",
    "days_remaining": "{days} days left",
    "month_contribution": "This month: {current} / {target}",
    "manage": "Manage →"
  },
  "reports": {
    "title": "Reports",
    "subtitle": "Monthly income and expense analysis.",
    "income": "This month — income",
    "expense": "This month — expense",
    "net": "This month — net",
    "txn_count": "{count} total transactions",
    "categories_count": "{count} categories with spending",
    "net_hint": "Income minus expense",
    "no_data": "No data",
    "no_data_desc": "No transactions this month.",
    "expense_by_category": "Expense by category",
    "current_month": "Current month"
  },
  "tasks": {
    "title": "Weekly Tasks",
    "subtitle": "Manage household tasks by week.",
    "add_task": "Add task",
    "no_tasks": "No tasks yet",
    "no_tasks_col": "No tasks",
    "note_placeholder": "Note…",
    "status_todo": "To do",
    "status_in_progress": "In progress",
    "status_done": "Done",
    "start": "Start",
    "done": "Done",
    "completed": "Completed",
    "category_shopping": "Shopping",
    "category_chores": "Chores",
    "category_finance": "Finance",
    "category_goal": "Goal",
    "assignee_label": "Assign to",
    "assignee_husband": "Husband",
    "assignee_wife": "Wife",
    "assignee_both": "Both",
    "delete_label": "Delete",
    "title_placeholder": "Task name…",
    "add_button": "+ Add"
  },
  "dates": {
    "title": "Anniversaries",
    "subtitle": "Save your special days.",
    "add_date": "+ Add date",
    "no_dates": "No anniversaries yet",
    "no_dates_desc": "Add special dates to receive reminders.",
    "upcoming": "Upcoming",
    "past": "Past",
    "view_calendar": "Calendar",
    "view_list": "List",
    "view_year": "Year",
    "days_until": "{days} days away",
    "days_ago": "{days} days ago",
    "today": "Today",
    "tomorrow": "Tomorrow",
    "edit": "Edit",
    "delete": "Delete"
  },
  "chat": {
    "title": "AI Assistant",
    "subtitle": "Chat with Concord AI about your family finances.",
    "placeholder": "Type a message…",
    "send": "Send",
    "thinking": "Thinking…",
    "empty_title": "Hello!",
    "empty_desc": "Start by entering a transaction, e.g. \"Bought coffee this morning 45k\"."
  },
  "dashboard": {
    "title": "Hello {name} 👋",
    "invite_title": "Invite your partner",
    "invite_desc": "Once both of you join, the system will create 3 spending funds (Husband / Wife / Joint) to get started.",
    "invite_cta": "Send invite link →",
    "year_goal": "Goal for {year}",
    "year_goal_desc": "Save {amount} together",
    "remaining": "Remaining",
    "days_remaining": "Days left",
    "days_remaining_value": "{days} days",
    "projection": "Projected end of period",
    "goal_reached": "Goal reached",
    "goal_missed": "Goal not reached",
    "goal_progress_hint": "Measured by transfers into savings & investment funds this year",
    "funds_balance": "Fund balances",
    "total_visible": "Total (visible to you): {amount}",
    "savings_investment": "Savings & investment",
    "recent_transactions": "Recent transactions",
    "add_via_chat": "+ Add via chat",
    "no_recent": "No transactions yet",
    "no_recent_desc": "Go to Chat to log transactions naturally — the agent will assign them to the right fund.",
    "target_funds": "Target funds"
  },
  "family": {
    "invite_title": "Invite member",
    "invite_subtitle": "Send an invite link for your partner to join.",
    "setup_title": "Family setup",
    "setup_subtitle": "Create your family to start managing finances together."
  }
}
```

- [ ] **Step 3: Verify build**

```bash
pnpm --filter web build
```

Expected: build thành công, không còn warning về missing messages.

- [ ] **Step 4: Commit**

```bash
git add apps/web/messages/
git commit -m "feat(web): add vi.json and en.json message files"
```

---

## Task 3: LocaleToggle component + wire vào Header

**Files:**
- Create: `apps/web/components/layout/locale-toggle.tsx`
- Modify: `apps/web/components/layout/header.tsx`

- [ ] **Step 1: Tạo `apps/web/components/layout/locale-toggle.tsx`**

```tsx
'use client';

import { useRouter } from 'next/navigation';
import { useLocale } from 'next-intl';

export function LocaleToggle() {
  const locale = useLocale();
  const router = useRouter();

  function switchLocale(next: 'vi' | 'en') {
    if (next === locale) return;
    document.cookie = `NEXT_LOCALE=${next};max-age=31536000;path=/`;
    router.refresh();
  }

  return (
    <div className="flex items-center overflow-hidden rounded-lg border border-stone-200 text-xs font-medium">
      <button
        type="button"
        onClick={() => switchLocale('vi')}
        className={`px-2 py-1 transition-colors ${
          locale === 'vi'
            ? 'bg-emerald-50 text-emerald-700'
            : 'text-stone-400 hover:text-stone-600'
        }`}
      >
        VI
      </button>
      <span className="text-stone-200">|</span>
      <button
        type="button"
        onClick={() => switchLocale('en')}
        className={`px-2 py-1 transition-colors ${
          locale === 'en'
            ? 'bg-emerald-50 text-emerald-700'
            : 'text-stone-400 hover:text-stone-600'
        }`}
      >
        EN
      </button>
    </div>
  );
}
```

- [ ] **Step 2: Sửa `apps/web/components/layout/header.tsx` — thêm LocaleToggle và useTranslations**

Thay toàn bộ nội dung file:

```tsx
'use client';

import Link from 'next/link';
import { useState } from 'react';
import { useTranslations } from 'next-intl';
import { UserAvatar } from '@/features/auth/components/user-avatar';
import type { AuthUser } from '@/features/auth/types';
import { LocaleToggle } from './locale-toggle';

export function Header({
  user,
  onLogout,
  onMenuClick,
}: {
  user: AuthUser;
  onLogout: () => void;
  onMenuClick?: () => void;
}) {
  const [open, setOpen] = useState(false);
  const t = useTranslations('nav');
  const tAuth = useTranslations('auth');

  return (
    <header className="flex h-16 items-center justify-between border-b border-stone-200 bg-white/80 px-3 backdrop-blur sm:px-4 lg:col-span-2 lg:px-6">
      <div className="flex items-center gap-3">
        {onMenuClick && (
          <button
            type="button"
            onClick={onMenuClick}
            className="-ml-1 flex h-9 w-9 items-center justify-center rounded-lg text-stone-700 hover:bg-stone-100 lg:hidden"
            aria-label="Mở menu"
          >
            <svg className="h-5 w-5" fill="none" viewBox="0 0 24 24" stroke="currentColor">
              <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M4 6h16M4 12h16M4 18h16" />
            </svg>
          </button>
        )}
        <div className="flex h-9 w-9 items-center justify-center rounded-lg bg-emerald-700 text-base font-bold text-white shadow-sm shadow-emerald-700/20">
          C
        </div>
        <div className="leading-tight">
          <div className="text-sm font-semibold text-stone-900">Concord</div>
          <div className="text-[11px] text-stone-500">Couple finance agent</div>
        </div>
      </div>

      <div className="flex items-center gap-2">
        <LocaleToggle />

        <div className="relative">
          <button
            onClick={() => setOpen((v) => !v)}
            className="flex items-center gap-2.5 rounded-lg border border-transparent px-2 py-1.5 transition-colors hover:border-stone-200 hover:bg-stone-50"
          >
            <UserAvatar user={user} size={32} />
            <div className="hidden text-left leading-tight sm:block">
              <div className="text-sm font-medium text-stone-800">{user.name}</div>
              <div className="text-[11px] text-stone-500">
                {user.role === 'husband' ? tAuth('husband') : tAuth('wife')} · {user.email}
              </div>
            </div>
            <svg
              className={`h-4 w-4 text-stone-400 transition-transform ${open ? 'rotate-180' : ''}`}
              fill="none" viewBox="0 0 24 24" stroke="currentColor"
            >
              <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M19 9l-7 7-7-7" />
            </svg>
          </button>

          {open && (
            <>
              <div className="fixed inset-0 z-10" onClick={() => setOpen(false)} />
              <div className="absolute right-0 z-20 mt-1 w-52 overflow-hidden rounded-lg border border-stone-200 bg-white shadow-lg">
                <Link
                  href="/settings"
                  onClick={() => setOpen(false)}
                  className="flex w-full items-center gap-2 px-3 py-2.5 text-left text-sm text-stone-700 transition-colors hover:bg-stone-50"
                >
                  <span>👤</span> {t('profile')}
                </Link>
                <div className="border-t border-stone-100" />
                <button
                  onClick={onLogout}
                  className="flex w-full items-center gap-2 px-3 py-2.5 text-left text-sm text-stone-700 transition-colors hover:bg-rose-50 hover:text-rose-700"
                >
                  <span>🚪</span> {t('logout', { ns: 'common' })}
                </button>
              </div>
            </>
          )}
        </div>
      </div>
    </header>
  );
}
```

**Lưu ý:** `t('logout', { ns: 'common' })` không đúng syntax next-intl. Dùng hook riêng:

```tsx
const tCommon = useTranslations('common');
// rồi dùng: tCommon('logout')
```

Sửa lại header để có thêm `const tCommon = useTranslations('common');` và dùng `tCommon('logout')` cho nút đăng xuất.

- [ ] **Step 3: Dev server test**

```bash
pnpm --filter web dev
```

Mở `http://localhost:3000`, kiểm tra:
- Toggle `VI | EN` hiện ở header, bên trái avatar
- Click `EN` → cookie `NEXT_LOCALE=en` được set → page refresh → toggle highlight `EN`
- Click `VI` → switch back

- [ ] **Step 4: Commit**

```bash
git add apps/web/components/layout/locale-toggle.tsx apps/web/components/layout/header.tsx
git commit -m "feat(web): add LocaleToggle component and wire into Header"
```

---

## Task 4: Sidebar i18n

**Files:**
- Modify: `apps/web/components/layout/sidebar.tsx`

- [ ] **Step 1: Sửa `apps/web/components/layout/sidebar.tsx`**

Thay toàn bộ file:

```tsx
'use client';

import Link from 'next/link';
import { usePathname } from 'next/navigation';
import { useTranslations } from 'next-intl';
import { FundCard } from '@/features/funds/components/fund-card';
import type { FundView } from '@/features/funds/types';

export function Sidebar({
  funds,
  onNavigate,
}: {
  funds: FundView[];
  onNavigate?: () => void;
}) {
  const pathname = usePathname();
  const t = useTranslations('nav');

  const NAV_GROUPS = [
    {
      label: t('overview'),
      items: [
        { href: '/dashboard', label: t('overview'), icon: '📊' },
        { href: '/chat', label: t('ai_assistant'), icon: '✨' },
      ],
    },
    {
      label: t('finance'),
      items: [
        { href: '/transactions', label: t('transactions'), icon: '💳' },
        { href: '/reports', label: t('reports'), icon: '📈' },
        { href: '/goals', label: t('savings_investment'), icon: '🏦' },
        { href: '/finance-settings', label: t('settings'), icon: '⚙️' },
      ],
    },
    {
      label: t('family_group'),
      items: [
        { href: '/weekly', label: t('tasks'), icon: '✅' },
        { href: '/important-dates', label: t('anniversaries'), icon: '🗓️' },
        { href: '/family/invite', label: t('members'), icon: '👨‍👩‍👦' },
      ],
    },
  ];

  function NavItem({ href, label, icon }: { href: string; label: string; icon: string }) {
    const active = pathname === href;
    return (
      <Link
        href={href}
        onClick={() => onNavigate?.()}
        className={`flex items-center gap-2.5 rounded-lg px-3 py-2 text-sm transition-colors ${
          active
            ? 'bg-emerald-50 font-medium text-emerald-800'
            : 'text-stone-600 hover:bg-stone-50 hover:text-stone-900'
        }`}
      >
        <span className="w-5 text-center text-base leading-none">{icon}</span>
        <span>{label}</span>
      </Link>
    );
  }

  return (
    <aside className="flex flex-col overflow-y-auto border-r border-stone-100 bg-white">
      <nav className="flex-1 px-3 py-4">
        {NAV_GROUPS.map((group) => (
          <div key={group.label} className="mb-5">
            <p className="mb-1 px-3 text-[10px] font-semibold uppercase tracking-widest text-stone-400">
              {group.label}
            </p>
            <div className="space-y-0.5">
              {group.items.map((item) => (
                <NavItem key={item.href} {...item} />
              ))}
            </div>
          </div>
        ))}
      </nav>

      <div className="border-t border-stone-100 px-3 py-3">
        <div className="mb-4 space-y-2">
          <p className="px-3 text-[10px] font-semibold uppercase tracking-widest text-stone-400">
            {t('your_funds')}
          </p>
          {funds.length === 0
            ? [1, 2].map((i) => (
                <div key={i} className="h-[68px] animate-pulse rounded-lg bg-stone-100" />
              ))
            : funds.map((f) => <FundCard key={f.id} fund={f} />)}
        </div>
      </div>
    </aside>
  );
}
```

- [ ] **Step 2: Test**

```bash
pnpm --filter web dev
```

Kiểm tra sidebar hiển thị đúng labels VI, switch sang EN xem labels đổi.

- [ ] **Step 3: Commit**

```bash
git add apps/web/components/layout/sidebar.tsx
git commit -m "feat(web): i18n sidebar nav labels"
```

---

## Task 5: Auth pages i18n (login, register, invite token, change-password modal)

**Files:**
- Modify: `apps/web/app/login/page.tsx`
- Modify: `apps/web/app/register/page.tsx` (nếu có)
- Modify: `apps/web/app/invite/[token]/page.tsx` (nếu có)
- Modify: `apps/web/features/auth/components/change-password-modal.tsx`

- [ ] **Step 1: Kiểm tra file register và invite tồn tại không**

```bash
ls apps/web/app/register/ 2>/dev/null && ls 'apps/web/app/invite/[token]/' 2>/dev/null
```

- [ ] **Step 2: Sửa `apps/web/app/login/page.tsx`**

Thêm `useTranslations` vào `LoginInner`:

```tsx
'use client';

import Link from 'next/link';
import { useRouter, useSearchParams } from 'next/navigation';
import { Suspense, useEffect, useState } from 'react';
import { useTranslations } from 'next-intl';
import { ApiError, setToken } from '@/lib/api-client';
import { login } from '@/features/auth/api';
import { useAuth } from '@/features/auth/hooks';

export default function LoginPage() {
  return (
    <Suspense fallback={null}>
      <LoginInner />
    </Suspense>
  );
}

function LoginInner() {
  const router = useRouter();
  const searchParams = useSearchParams();
  const next = searchParams.get('next');
  const { state: auth } = useAuth(false);
  const t = useTranslations('auth');
  const [email, setEmail] = useState('');
  const [password, setPassword] = useState('');
  const [error, setError] = useState<string | null>(null);
  const [submitting, setSubmitting] = useState(false);

  useEffect(() => {
    if (auth.status === 'authed') {
      router.replace(next ?? (auth.user.familyId ? '/dashboard' : '/family/setup'));
    }
  }, [auth, next, router]);

  async function onSubmit(e: React.FormEvent) {
    e.preventDefault();
    setError(null);
    setSubmitting(true);
    try {
      const res = await login(email.trim(), password);
      setToken(res.accessToken);
      if (next) {
        router.replace(next);
      } else if (!res.user.familyId) {
        router.replace('/family/setup');
      } else {
        router.replace('/dashboard');
      }
    } catch (err) {
      const msg =
        err instanceof ApiError
          ? err.message
          : err instanceof Error
            ? err.message
            : 'Đăng nhập thất bại';
      setError(msg);
    } finally {
      setSubmitting(false);
    }
  }

  return (
    <main className="relative flex min-h-screen items-center justify-center overflow-hidden bg-stone-50 px-4 py-12">
      <div aria-hidden className="pointer-events-none absolute inset-0 bg-gradient-to-br from-emerald-50 via-stone-50 to-amber-50" />
      <div aria-hidden className="pointer-events-none absolute -top-40 -right-40 h-96 w-96 rounded-full bg-emerald-200/40 blur-3xl" />
      <div aria-hidden className="pointer-events-none absolute -bottom-40 -left-40 h-96 w-96 rounded-full bg-amber-200/40 blur-3xl" />

      <div className="relative w-full max-w-md">
        <div className="mb-8 text-center">
          <div className="mb-3 inline-flex h-14 w-14 items-center justify-center rounded-2xl bg-emerald-700 text-2xl font-bold text-white shadow-lg shadow-emerald-700/20">
            C
          </div>
          <h1 className="text-3xl font-semibold tracking-tight text-stone-900">Concord</h1>
          <p className="mt-1 text-sm text-stone-500">{t('login_tagline')}</p>
        </div>

        <div className="rounded-2xl border border-stone-200/80 bg-white/90 p-6 shadow-xl shadow-stone-300/30 backdrop-blur sm:p-8">
          <h2 className="text-base font-semibold text-stone-800">{t('login_title')}</h2>
          <p className="mt-1 mb-6 text-xs text-stone-500">{t('login_subtitle')}</p>

          <form onSubmit={onSubmit} className="space-y-4">
            <div>
              <label className="mb-1.5 block text-xs font-medium text-stone-700">{t('email')}</label>
              <input
                type="email"
                value={email}
                onChange={(e) => setEmail(e.target.value)}
                required
                autoFocus
                placeholder="ban@gmail.com"
                className="w-full rounded-lg border border-stone-200 bg-stone-50 px-3.5 py-2.5 text-sm transition-colors placeholder:text-stone-400 focus:border-emerald-500 focus:bg-white focus:outline-none focus:ring-2 focus:ring-emerald-100"
                disabled={submitting}
              />
            </div>

            <div>
              <label className="mb-1.5 block text-xs font-medium text-stone-700">{t('password')}</label>
              <input
                type="password"
                value={password}
                onChange={(e) => setPassword(e.target.value)}
                required
                placeholder="••••••••"
                className="w-full rounded-lg border border-stone-200 bg-stone-50 px-3.5 py-2.5 text-sm transition-colors placeholder:text-stone-400 focus:border-emerald-500 focus:bg-white focus:outline-none focus:ring-2 focus:ring-emerald-100"
                disabled={submitting}
              />
            </div>

            {error && (
              <div className="rounded-lg border border-rose-200 bg-rose-50 px-3 py-2 text-xs text-rose-800">
                {error}
              </div>
            )}

            <button
              type="submit"
              disabled={submitting || !email || !password}
              className="group relative w-full overflow-hidden rounded-lg bg-emerald-700 px-4 py-2.5 text-sm font-medium text-white shadow-sm transition-all hover:bg-emerald-800 active:scale-[0.99] disabled:cursor-not-allowed disabled:bg-stone-300"
            >
              {submitting ? t('submitting') : t('login_title')}
            </button>
          </form>

          <p className="mt-4 text-center text-xs text-stone-500">
            {t('no_account')}{' '}
            <Link href="/register" className="font-medium text-emerald-700 hover:underline">
              {t('register_link')}
            </Link>
          </p>
        </div>
      </div>
    </main>
  );
}
```

- [ ] **Step 3: Sửa `apps/web/features/auth/components/change-password-modal.tsx`**

Đọc file hiện tại trước:

```bash
cat apps/web/features/auth/components/change-password-modal.tsx
```

Thêm `const t = useTranslations('auth');` vào đầu component function, thay các string tiếng Việt bằng `t('key')` tương ứng:
- `'Đổi mật khẩu'` → `t('change_password')`
- `'Mật khẩu hiện tại'` → `t('current_password')`
- `'Mật khẩu mới'` → `t('new_password')`
- `'Đổi mật khẩu thành công'` → `t('change_password_success')`
- `'Đang lưu…'` → `useTranslations('common')('saving')`
- `'Huỷ'` → `useTranslations('common')('cancel')`

- [ ] **Step 4: Test login page**

```bash
pnpm --filter web dev
```

Mở `http://localhost:3000/login`, switch VI/EN, kiểm tra labels đổi ngôn ngữ.

- [ ] **Step 5: Commit**

```bash
git add apps/web/app/login/page.tsx apps/web/features/auth/components/change-password-modal.tsx
git commit -m "feat(web): i18n auth pages and change-password modal"
```

---

## Task 6: Settings page i18n

**Files:**
- Modify: `apps/web/app/(authed)/settings/page.tsx`

- [ ] **Step 1: Sửa `apps/web/app/(authed)/settings/page.tsx`**

Thêm vào `AccountSection`:
```tsx
const t = useTranslations('settings');
const tAuth = useTranslations('auth');
const tCommon = useTranslations('common');
```

Thay các strings:
- `'Cài đặt'` (title) → `t('title')`
- `'Tài khoản, mật khẩu và quy tắc lương'` → `t('subtitle')`
- `'Tài khoản'` → `t('account')`
- `'Concord là couple-only…'` → `t('couple_only_note')`
- `'✏️ Chỉnh sửa'` → `t('click_edit')`
- `'Bấm vào ảnh để thay đổi'` → `t('click_to_change_avatar')`
- `'Chỉnh sửa JPEG, PNG hoặc WebP'` (avatar error) → hardcode vì đây là validation message
- `'Vai trò'` label → `t('role_label')`
- `'Email'` label → `t('email_label')`
- `'Ngày sinh'` label → `t('birthdate_label')`
- `'ID'` label → `t('id_label')`
- `'Lưu'` → `tCommon('save')`
- `'Đang lưu…'` → `tCommon('saving')`
- `'Huỷ'` → `tCommon('cancel')`
- `'User ID:'` → `t('user_id') + ':'`
- `user.role === 'husband' ? 'Chồng' : 'Vợ'` → `user.role === 'husband' ? tAuth('husband') : tAuth('wife')`

- [ ] **Step 2: Commit**

```bash
git add 'apps/web/app/(authed)/settings/page.tsx'
git commit -m "feat(web): i18n settings page"
```

---

## Task 7: Finance settings page i18n

**Files:**
- Modify: `apps/web/app/(authed)/finance-settings/page.tsx`

- [ ] **Step 1: Sửa `apps/web/app/(authed)/finance-settings/page.tsx`**

Thêm vào đầu mỗi component function cần translate:
```tsx
const t = useTranslations('finance');
const tCommon = useTranslations('common');
```

Thay các strings:
- `'Cài đặt tài chính'` → `t('title')`
- `'Mục tiêu tiết kiệm năm và số dư khởi đầu các quỹ.'` → `t('subtitle')`
- `'Mục tiêu tiết kiệm năm {year}'` → `t('yearly_goal_title', { year })`
- `'Số tiền 2 vợ chồng muốn để dành…'` → `t('yearly_goal_desc')`
- `'Mục tiêu (VND)'` → `t('goal_vnd')`
- `'vd: 150000000 (150 triệu)'` → `t('goal_placeholder')`
- `'Nhập số nguyên VND, vd: 150000000.'` → `t('goal_hint')`
- `'Hiển thị: '` → `t('goal_preview') + ' '`
- `'Đã lưu mục tiêu'` → `t('goal_saved')`
- `'Số dư khởi đầu'` → `t('opening_balance_title')`
- `'Tiền đã có sẵn…'` → `t('opening_balance_desc')`
- `'Số dư hiện tại: '` → `t('current_balance') + ': '`
- `'Đã lưu'` → `t('balance_saved')`
- `'Lưu'` → `tCommon('save')`
- `'Đang lưu…'` → `tCommon('saving')`
- `'Reset'` → `tCommon('reset')`

- [ ] **Step 2: Commit**

```bash
git add 'apps/web/app/(authed)/finance-settings/page.tsx'
git commit -m "feat(web): i18n finance-settings page"
```

---

## Task 8: Transactions page và components i18n

**Files:**
- Modify: `apps/web/app/(authed)/transactions/page.tsx`
- Modify: `apps/web/features/transactions/components/edit-transaction-modal.tsx`
- Modify: `apps/web/features/transactions/components/day-group.tsx`
- Modify: `apps/web/features/transactions/components/month-switcher.tsx`

- [ ] **Step 1: Đọc các file để xác định strings cần thay**

```bash
cat 'apps/web/app/(authed)/transactions/page.tsx' | head -80
cat apps/web/features/transactions/components/edit-transaction-modal.tsx | head -60
```

- [ ] **Step 2: Sửa `apps/web/app/(authed)/transactions/page.tsx`**

Thêm:
```tsx
const t = useTranslations('transactions');
const tCommon = useTranslations('common');
```

Thay:
- `'Giao dịch'` (title) → `t('title')`
- `'Lịch sử thu chi theo quỹ.'` → `t('subtitle')`
- `'Tất cả quỹ'` → `t('all_funds')`
- Empty state: `'Chưa có giao dịch nào'` → `t('no_transactions')`

- [ ] **Step 3: Sửa `apps/web/features/transactions/components/edit-transaction-modal.tsx`**

Thêm:
```tsx
const t = useTranslations('transactions');
const tCommon = useTranslations('common');
```

Thay strings: title modal, labels, buttons.

- [ ] **Step 4: Sửa components còn lại (day-group, month-switcher)**

Trong `month-switcher.tsx`:
```tsx
const t = useTranslations('transactions');
// 'Tháng trước' → t('prev_month'), 'Tháng sau' → t('next_month')
```

- [ ] **Step 5: Commit**

```bash
git add 'apps/web/app/(authed)/transactions/page.tsx' apps/web/features/transactions/components/
git commit -m "feat(web): i18n transactions page and components"
```

---

## Task 9: Goals page i18n

**Files:**
- Modify: `apps/web/app/(authed)/goals/page.tsx`

- [ ] **Step 1: Đọc file**

```bash
cat 'apps/web/app/(authed)/goals/page.tsx' | head -100
```

- [ ] **Step 2: Sửa file**

Thêm:
```tsx
const t = useTranslations('goals');
const tCommon = useTranslations('common');
```

Thay:
- `'Quỹ tiết kiệm & đầu tư'` → `t('title')`
- `'Tích luỳ theo mục tiêu cụ thể…'` → `t('subtitle')`
- `'+ Tạo quỹ'` → `t('create_fund')`
- `'Đang vượt tiến độ'` → `t('ahead')`
- `'Đúng tiến độ'` → `t('on_track')`
- `'Đang chậm'` → `t('behind')`
- `'Còn {n} ngày'` → `t('days_remaining', { days: n })`
- `'Lưu'` / `'Huỷ'` → `tCommon('save')` / `tCommon('cancel')`

- [ ] **Step 3: Commit**

```bash
git add 'apps/web/app/(authed)/goals/page.tsx'
git commit -m "feat(web): i18n goals page"
```

---

## Task 10: Reports page i18n

**Files:**
- Modify: `apps/web/app/(authed)/reports/page.tsx`

- [ ] **Step 1: Đọc file**

```bash
cat 'apps/web/app/(authed)/reports/page.tsx'
```

- [ ] **Step 2: Sửa file**

Thêm:
```tsx
const t = useTranslations('reports');
```

Thay:
- `'Báo cáo'` → `t('title')`
- `'Phân tích thu chi theo tháng.'` → `t('subtitle')`
- `'Tháng này — thu'` → `t('income')`
- `'Tháng này — chi'` → `t('expense')`
- `'Tháng này — net'` → `t('net')`
- `'{n} giao dịch tổng'` → `t('txn_count', { count: n })`
- `'Thu trừ chi'` → `t('net_hint')`
- `'Chi tiêu theo danh mục'` → `t('expense_by_category')`
- Empty state → `t('no_data')` + `t('no_data_desc')`

- [ ] **Step 3: Commit**

```bash
git add 'apps/web/app/(authed)/reports/page.tsx'
git commit -m "feat(web): i18n reports page"
```

---

## Task 11: Tasks (weekly board) i18n

**Files:**
- Modify: `apps/web/app/(authed)/weekly/page.tsx`
- Modify: `apps/web/features/tasks/components/task-board.tsx`
- Modify: `apps/web/features/tasks/components/task-card.tsx`
- Modify: `apps/web/features/tasks/components/task-quick-add.tsx`

- [ ] **Step 1: Đọc weekly/page.tsx**

```bash
cat 'apps/web/app/(authed)/weekly/page.tsx'
```

- [ ] **Step 2: Sửa `apps/web/app/(authed)/weekly/page.tsx`**

Thêm `useTranslations('tasks')`, thay title/subtitle.

- [ ] **Step 3: Sửa `apps/web/features/tasks/components/task-board.tsx`**

Thêm `useTranslations('tasks')`. Thay:
- Column headers: `'Cần làm'` → `t('status_todo')`, `'Đang làm'` → `t('status_in_progress')`, `'Xong'` → `t('status_done')`
- Empty cell: `'Chưa có việc'` → `t('no_tasks_col')`

- [ ] **Step 4: Sửa `apps/web/features/tasks/components/task-card.tsx`**

Thêm `useTranslations('tasks')`. Thay:
- Category labels: dùng `t('category_shopping')`, v.v.
- `'Bắt đầu'` → `t('start')`
- `'Xong'` (button) → `t('done')`
- `'Hoàn thành'` (badge) → `t('completed')`
- `'Ghi chú…'` → `t('note_placeholder')`
- `'Hủy'` → `useTranslations('common')('cancel')`
- `'Lưu'` → `useTranslations('common')('save')`

- [ ] **Step 5: Sửa `apps/web/features/tasks/components/task-quick-add.tsx`**

Thêm `useTranslations('tasks')`. Thay:
- `'Thêm việc'` → `t('add_task')`
- Assignee labels: `t('assignee_husband')`, `t('assignee_wife')`, `t('assignee_both')`
- Category labels: `t('category_shopping')`, v.v.
- Input placeholder → `t('title_placeholder')`
- Submit button → `t('add_button')`

**Lưu ý quan trọng:** `task-card.tsx` và `task-quick-add.tsx` hiện dùng object `CATEGORY_CONFIG` với hardcoded labels. Thay pattern:

```tsx
// Trước:
const CATEGORY_CONFIG = {
  shopping: { label: 'Mua sắm', ... },
  ...
}

// Sau (trong component function):
function TaskCard(...) {
  const t = useTranslations('tasks');
  const CATEGORY_LABELS: Record<string, string> = {
    shopping: t('category_shopping'),
    chores: t('category_chores'),
    finance: t('category_finance'),
    goal: t('category_goal'),
  };
  const cat = { ...CATEGORY_CONFIG[task.category], label: CATEGORY_LABELS[task.category] };
  ...
}
```

- [ ] **Step 6: Commit**

```bash
git add 'apps/web/app/(authed)/weekly/page.tsx' apps/web/features/tasks/components/
git commit -m "feat(web): i18n tasks (weekly board) page and components"
```

---

## Task 12: Important dates i18n

**Files:**
- Modify: `apps/web/app/(authed)/important-dates/page.tsx`
- Modify: `apps/web/app/(authed)/important-dates/calendar/page.tsx`
- Modify: `apps/web/app/(authed)/important-dates/year/page.tsx`
- Modify: `apps/web/features/important-dates/components/` (tất cả components)

- [ ] **Step 1: Liệt kê components**

```bash
ls apps/web/features/important-dates/components/
```

- [ ] **Step 2: Đọc và sửa từng file**

Với mỗi file, thêm `const t = useTranslations('dates');` và thay strings theo map:
- `'Ngày kỷ niệm'` → `t('title')`
- `'+ Thêm ngày'` → `t('add_date')`
- `'Chưa có ngày kỷ niệm nào'` → `t('no_dates')`
- `'còn {n} ngày'` → `t('days_until', { days: n })`
- `'{n} ngày trước'` → `t('days_ago', { days: n })`
- `'Hôm nay'` → `t('today')`
- `'Ngày mai'` → `t('tomorrow')`
- View tabs: `t('view_calendar')`, `t('view_list')`, `t('view_year')`

- [ ] **Step 3: Commit**

```bash
git add 'apps/web/app/(authed)/important-dates/' apps/web/features/important-dates/
git commit -m "feat(web): i18n important-dates pages and components"
```

---

## Task 13: Chat page i18n

**Files:**
- Modify: `apps/web/app/(authed)/chat/page.tsx`

- [ ] **Step 1: Đọc file**

```bash
cat 'apps/web/app/(authed)/chat/page.tsx'
```

- [ ] **Step 2: Sửa file**

Thêm `const t = useTranslations('chat');`. Thay:
- `'Trợ lý AI'` → `t('title')`
- Subtitle → `t('subtitle')`
- Placeholder input → `t('placeholder')`
- `'Gửi'` → `t('send')`
- `'Đang suy nghĩ…'` → `t('thinking')`
- Empty state → `t('empty_title')`, `t('empty_desc')`

- [ ] **Step 3: Commit**

```bash
git add 'apps/web/app/(authed)/chat/page.tsx'
git commit -m "feat(web): i18n chat page"
```

---

## Task 14: Dashboard page i18n

**Files:**
- Modify: `apps/web/app/(authed)/dashboard/page.tsx`

- [ ] **Step 1: Sửa file**

Dashboard có nhiều strings phức tạp. Thêm:
```tsx
const t = useTranslations('dashboard');
const tGoals = useTranslations('goals');
const tCommon = useTranslations('common');
```

Thay:
- `\`Chào ${user.name} 👋\`` → `t('title', { name: user.name })`
- Hôm nay là... → giữ nguyên `new Date().toLocaleDateString(locale === 'en' ? 'en-US' : 'vi-VN', ...)` — cần đọc locale: `const locale = useLocale();`
- `'Mời vợ/chồng tham gia gia đình'` → `t('invite_title')`
- `'Sau khi 2 người...'` → `t('invite_desc')`
- `'Gửi link mời ngay →'` → `t('invite_cta')`
- `'Mục tiêu năm {year}'` → `t('year_goal', { year })`
- `'Tiết kiệm {amount} cùng nhau'` → `t('year_goal_desc', { amount: formatVND(goal.targetAmount) })`
- paceLabel map → dùng `tGoals('ahead')`, `tGoals('on_track')`, `tGoals('behind')`
- `'Còn lại'` → `t('remaining')`
- `'Còn ngày'` → `t('days_remaining')`
- `'{n} ngày'` → `t('days_remaining_value', { days: goal.daysRemaining })`
- `'Dự kiến cuối kỳ'` → `t('projection')`
- `'Đạt mục tiêu'` → `t('goal_reached')`
- `'Hụt mục tiêu'` → `t('goal_missed')`
- `'Tính theo dòng tiền...'` → `t('goal_progress_hint')`
- `'Số dư quỹ'` → `t('funds_balance')`
- `'Tổng (bạn thấy được): {amount}'` → `t('total_visible', { amount: formatVND(totalVisible) })`
- `'Tiết kiệm & đầu tư'` → `t('savings_investment')`
- `'Giao dịch gần đây'` → `t('recent_transactions')`
- `'+ Thêm qua chat'` → `t('add_via_chat')`
- `'Chưa có giao dịch nào'` → `t('no_recent')`
- `'Vào tab Chat...'` → `t('no_recent_desc')`
- `'Quỹ mục tiêu'` → `t('target_funds')`
- confirm/alert strings → `tCommon('confirm_delete_transaction')`, `tCommon('cannot_delete', { msg })`

Thêm `import { useLocale } from 'next-intl';` và `const locale = useLocale();` để format date đúng locale.

- [ ] **Step 2: Commit**

```bash
git add 'apps/web/app/(authed)/dashboard/page.tsx'
git commit -m "feat(web): i18n dashboard page"
```

---

## Task 15: Family pages i18n

**Files:**
- Modify: `apps/web/app/(authed)/family/setup/page.tsx`
- Modify: `apps/web/app/(authed)/family/invite/page.tsx`

- [ ] **Step 1: Đọc files**

```bash
cat 'apps/web/app/(authed)/family/setup/page.tsx'
cat 'apps/web/app/(authed)/family/invite/page.tsx'
```

- [ ] **Step 2: Sửa cả hai files**

Thêm `const t = useTranslations('family');` và `const tCommon = useTranslations('common');`. Thay strings theo map trong `family` namespace.

- [ ] **Step 3: Commit**

```bash
git add 'apps/web/app/(authed)/family/'
git commit -m "feat(web): i18n family pages"
```

---

## Task 16: Final verification

- [ ] **Step 1: Build production**

```bash
pnpm --filter web build
```

Expected: build thành công, 0 TypeScript errors, 0 missing translation keys.

- [ ] **Step 2: Smoke test VI → EN → VI**

```bash
pnpm --filter web dev
```

- Mở `http://localhost:3000/login` — kiểm tra tiếng Việt
- Click `EN` — toàn bộ text đổi sang tiếng Anh
- Navigate qua: dashboard, transactions, goals, reports, weekly, important-dates, chat, settings, finance-settings
- Click `VI` — text đổi về tiếng Việt

- [ ] **Step 3: Kiểm tra cookie persist**

- Chọn `EN`, đóng tab, mở lại `http://localhost:3000` — vẫn hiện tiếng Anh

- [ ] **Step 4: Commit cuối**

```bash
git add .
git commit -m "feat(web): complete i18n vi/en with next-intl"
```
