# Dark Mode Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Thêm dark mode (Sáng / Tối / Hệ thống) cho toàn bộ Concord web app, migrate raw Tailwind color classes sang semantic token system.

**Architecture:** CSS variables trong `globals.css` (light + `.dark` palettes) expose qua Tailwind v4 `@theme inline`. `next-themes` quản state + persist localStorage + handle FOUC. Toggle nằm ở header (desktop) và mobile drawer. Refactor ~22 file `.tsx` đổi raw color classes (`bg-stone-50`, `text-gray-600`...) sang semantic tokens (`bg-background`, `text-muted-foreground`...).

**Tech Stack:** NextJS 16 App Router, Tailwind v4, `next-themes`, lucide-react (already in repo).

**Spec:** [docs/superpowers/specs/2026-05-14-dark-mode-design.md](../specs/2026-05-14-dark-mode-design.md)

---

## Phase 1: Foundation — Tokens, Provider, Toggle

### Task 1: Define color tokens trong globals.css

**Files:**
- Modify: `apps/web/app/globals.css`

- [ ] **Step 1: Read current globals.css**

Run: `cat apps/web/app/globals.css`
Expected: nội dung hiện tại bắt đầu với `@import "tailwindcss";` và `@theme inline { --font-sans... }`.

- [ ] **Step 2: Replace globals.css content**

Replace toàn bộ file `apps/web/app/globals.css` với:

```css
@import "tailwindcss";

@custom-variant dark (&:where(.dark, .dark *));

:root {
  --background: oklch(0.99 0.003 90);
  --foreground: oklch(0.21 0.01 90);
  --card: oklch(1 0 0);
  --card-foreground: oklch(0.21 0.01 90);
  --popover: oklch(1 0 0);
  --popover-foreground: oklch(0.21 0.01 90);
  --muted: oklch(0.96 0.005 90);
  --muted-foreground: oklch(0.45 0.01 90);
  --border: oklch(0.92 0.005 90);
  --input: oklch(0.92 0.005 90);
  --ring: oklch(0.45 0.15 250);
  --primary: oklch(0.45 0.15 250);
  --primary-foreground: oklch(0.99 0 0);
  --secondary: oklch(0.96 0.005 90);
  --secondary-foreground: oklch(0.21 0.01 90);
  --accent: oklch(0.96 0.005 90);
  --accent-foreground: oklch(0.21 0.01 90);
  --destructive: oklch(0.55 0.20 25);
  --destructive-foreground: oklch(0.99 0 0);

  --chat-private-bg: oklch(0.98 0.01 280);
  --chat-public-bg: oklch(0.98 0.015 140);
}

.dark {
  --background: oklch(0.16 0.01 250);
  --foreground: oklch(0.95 0.005 90);
  --card: oklch(0.20 0.01 250);
  --card-foreground: oklch(0.95 0.005 90);
  --popover: oklch(0.20 0.01 250);
  --popover-foreground: oklch(0.95 0.005 90);
  --muted: oklch(0.24 0.01 250);
  --muted-foreground: oklch(0.65 0.01 90);
  --border: oklch(0.28 0.01 250);
  --input: oklch(0.28 0.01 250);
  --ring: oklch(0.65 0.18 250);
  --primary: oklch(0.65 0.18 250);
  --primary-foreground: oklch(0.16 0.01 250);
  --secondary: oklch(0.24 0.01 250);
  --secondary-foreground: oklch(0.95 0.005 90);
  --accent: oklch(0.24 0.01 250);
  --accent-foreground: oklch(0.95 0.005 90);
  --destructive: oklch(0.60 0.22 25);
  --destructive-foreground: oklch(0.99 0 0);

  --chat-private-bg: oklch(0.22 0.02 280);
  --chat-public-bg: oklch(0.22 0.02 140);
}

@theme inline {
  --font-sans: var(--font-sans);
  --font-mono: var(--font-mono);

  --color-background: var(--background);
  --color-foreground: var(--foreground);
  --color-card: var(--card);
  --color-card-foreground: var(--card-foreground);
  --color-popover: var(--popover);
  --color-popover-foreground: var(--popover-foreground);
  --color-muted: var(--muted);
  --color-muted-foreground: var(--muted-foreground);
  --color-border: var(--border);
  --color-input: var(--input);
  --color-ring: var(--ring);
  --color-primary: var(--primary);
  --color-primary-foreground: var(--primary-foreground);
  --color-secondary: var(--secondary);
  --color-secondary-foreground: var(--secondary-foreground);
  --color-accent: var(--accent);
  --color-accent-foreground: var(--accent-foreground);
  --color-destructive: var(--destructive);
  --color-destructive-foreground: var(--destructive-foreground);
}

html {
  font-feature-settings: "cv11", "ss01";
}

body {
  font-family: var(--font-sans), -apple-system, BlinkMacSystemFont,
    "Segoe UI", Roboto, sans-serif;
}

@media (max-width: 640px) {
  input,
  textarea,
  select {
    font-size: 16px;
  }
}
```

- [ ] **Step 3: Verify dev server compiles**

Run: `pnpm --filter web dev` (background) → mở http://localhost:3000
Expected: server start không lỗi; UI vẫn light mode (chưa có dark class nào trên html), nhưng các utility class hiện tại (`bg-stone-50`...) vẫn render bình thường vì chưa migrate.

Stop dev server sau khi verify.

- [ ] **Step 4: Commit**

```bash
git add apps/web/app/globals.css
git commit -m "feat(web): add semantic color tokens + dark palette in globals.css"
```

---

### Task 2: Cài next-themes

**Files:**
- Modify: `apps/web/package.json`, root `pnpm-lock.yaml`

- [ ] **Step 1: Install next-themes**

Run: `pnpm --filter web add next-themes`
Expected: `next-themes` xuất hiện trong `apps/web/package.json` dependencies (version ^0.4 hoặc latest).

- [ ] **Step 2: Verify install**

Run: `cat apps/web/package.json | grep next-themes`
Expected: dòng `"next-themes": "^x.y.z"`.

- [ ] **Step 3: Commit**

```bash
git add apps/web/package.json pnpm-lock.yaml
git commit -m "chore(web): add next-themes dependency"
```

---

### Task 3: Wrap ThemeProvider trong root layout

**Files:**
- Modify: `apps/web/app/layout.tsx`

- [ ] **Step 1: Replace layout.tsx**

Replace toàn bộ file `apps/web/app/layout.tsx` với:

```tsx
import type { Metadata } from 'next';
import { Be_Vietnam_Pro, JetBrains_Mono } from 'next/font/google';
import { NextIntlClientProvider } from 'next-intl';
import { getLocale, getMessages } from 'next-intl/server';
import { ThemeProvider } from 'next-themes';
import './globals.css';

const beVietnamPro = Be_Vietnam_Pro({
  variable: '--font-sans',
  subsets: ['latin', 'vietnamese'],
  weight: ['400', '500', '600', '700'],
  display: 'swap',
});

const jetBrainsMono = JetBrains_Mono({
  variable: '--font-mono',
  subsets: ['latin', 'vietnamese'],
  display: 'swap',
});

export const metadata: Metadata = {
  title: 'Concord — Couple Finance Agent',
  description:
    'Track expenses across personal + joint funds. Hit your savings goals with an AI agent.',
};

export default async function RootLayout({
  children,
}: Readonly<{ children: React.ReactNode }>) {
  const locale = await getLocale();
  const messages = await getMessages();
  return (
    <html
      lang={locale}
      suppressHydrationWarning
      className={`${beVietnamPro.variable} ${jetBrainsMono.variable} h-full antialiased`}
    >
      <body className="min-h-full bg-background font-sans text-foreground">
        <ThemeProvider attribute="class" defaultTheme="system" enableSystem disableTransitionOnChange>
          <NextIntlClientProvider locale={locale} messages={messages}>
            {children}
          </NextIntlClientProvider>
        </ThemeProvider>
      </body>
    </html>
  );
}
```

Notes:
- `suppressHydrationWarning` cần vì next-themes set class trước hydrate.
- Body classes đổi `bg-stone-50 text-stone-900` → `bg-background text-foreground`.
- `disableTransitionOnChange` tránh nháy các transition khi toggle theme.

- [ ] **Step 2: Verify dev server reload**

Run: `pnpm --filter web dev` (background) → reload http://localhost:3000
Expected: page render bình thường, không lỗi hydration trong console.

Mở DevTools → Application → Local Storage → check key `theme` = `system` (next-themes mặc định).

Stop dev server.

- [ ] **Step 3: Commit**

```bash
git add apps/web/app/layout.tsx
git commit -m "feat(web): wrap root layout with next-themes ThemeProvider"
```

---

### Task 4: Tạo ThemeToggle component

**Files:**
- Create: `apps/web/components/layout/theme-toggle.tsx`

- [ ] **Step 1: Create theme-toggle.tsx**

Tạo file `apps/web/components/layout/theme-toggle.tsx`:

```tsx
'use client';

import { useEffect, useState } from 'react';
import { useTheme } from 'next-themes';
import { Monitor, Moon, Sun } from 'lucide-react';
import { useTranslations } from 'next-intl';

type ThemeOption = 'light' | 'dark' | 'system';

export function ThemeToggle() {
  const { theme, setTheme } = useTheme();
  const [mounted, setMounted] = useState(false);
  const [open, setOpen] = useState(false);
  const t = useTranslations('common');

  useEffect(() => setMounted(true), []);

  if (!mounted) {
    return <div className="h-9 w-9" aria-hidden />;
  }

  const current = (theme ?? 'system') as ThemeOption;
  const Icon = current === 'light' ? Sun : current === 'dark' ? Moon : Monitor;

  const options: { value: ThemeOption; label: string; icon: typeof Sun }[] = [
    { value: 'light', label: t('themeLight'), icon: Sun },
    { value: 'dark', label: t('themeDark'), icon: Moon },
    { value: 'system', label: t('themeSystem'), icon: Monitor },
  ];

  return (
    <div className="relative">
      <button
        type="button"
        onClick={() => setOpen((v) => !v)}
        onPointerDown={(e) => e.stopPropagation()}
        className="flex h-9 w-9 items-center justify-center rounded-lg text-muted-foreground hover:bg-muted hover:text-foreground"
        aria-label={t('themeToggle')}
      >
        <Icon className="h-4 w-4" />
      </button>
      {open && (
        <>
          <div
            className="fixed inset-0 z-40"
            onClick={() => setOpen(false)}
            aria-hidden
          />
          <div
            role="menu"
            className="absolute right-0 z-50 mt-1 w-40 overflow-hidden rounded-lg border border-border bg-popover text-popover-foreground shadow-lg"
          >
            {options.map((opt) => {
              const OptIcon = opt.icon;
              const active = current === opt.value;
              return (
                <button
                  key={opt.value}
                  type="button"
                  role="menuitem"
                  onClick={() => {
                    setTheme(opt.value);
                    setOpen(false);
                  }}
                  className={`flex w-full items-center gap-2 px-3 py-2 text-left text-sm hover:bg-muted ${
                    active ? 'bg-muted font-medium' : ''
                  }`}
                >
                  <OptIcon className="h-4 w-4" />
                  {opt.label}
                </button>
              );
            })}
          </div>
        </>
      )}
    </div>
  );
}
```

- [ ] **Step 2: Add i18n keys**

Find existing translation files. Run: `find apps/web -name "*.json" -path "*/messages/*"`
Expected: ít nhất 1 file (vd `apps/web/messages/vi.json` và/hoặc `en.json`).

Add 4 keys vào namespace `common` trong mỗi file:

Trong file vi.json (locale vi):
```json
"themeToggle": "Đổi giao diện",
"themeLight": "Sáng",
"themeDark": "Tối",
"themeSystem": "Hệ thống"
```

Trong file en.json (nếu có):
```json
"themeToggle": "Toggle theme",
"themeLight": "Light",
"themeDark": "Dark",
"themeSystem": "System"
```

- [ ] **Step 3: Commit**

```bash
git add apps/web/components/layout/theme-toggle.tsx apps/web/messages/
git commit -m "feat(web): add ThemeToggle dropdown component (light/dark/system)"
```

---

### Task 5: Mount ThemeToggle vào Header

**Files:**
- Modify: `apps/web/components/layout/header.tsx`

- [ ] **Step 1: Add import**

Mở `apps/web/components/layout/header.tsx`. Sau dòng `import { LocaleToggle } from './locale-toggle';` thêm:

```tsx
import { ThemeToggle } from './theme-toggle';
```

- [ ] **Step 2: Mount toggle**

Tìm block:
```tsx
<div className="flex items-center gap-2">
  <LocaleToggle />
```

Thêm `<ThemeToggle />` ngay sau `<LocaleToggle />`:
```tsx
<div className="flex items-center gap-2">
  <LocaleToggle />
  <ThemeToggle />
```

- [ ] **Step 3: Verify in browser**

Run: `pnpm --filter web dev`
Mở http://localhost:3000/login (hoặc trang nào dùng Header). Login nếu cần.
Expected: icon theme (Monitor mặc định = system mode) hiện cạnh LocaleToggle. Click → dropdown 3 option. Chọn Tối → `<html class>` thêm `dark`. Chọn Sáng → bỏ. Chọn Hệ thống → follow OS.

Lưu ý: hầu hết page sẽ vẫn light-looking vì chưa migrate. OK miễn là toggle gắn `.dark` class lên html.

Stop dev server.

- [ ] **Step 4: Commit**

```bash
git add apps/web/components/layout/header.tsx
git commit -m "feat(web): mount ThemeToggle in app header"
```

---

### Task 6: Mount ThemeToggle vào mobile-drawer

**Files:**
- Modify: `apps/web/components/ui/mobile-drawer.tsx`

- [ ] **Step 1: Read current file**

Run: `cat apps/web/components/ui/mobile-drawer.tsx`
Locate area near top of drawer (cạnh close button) hoặc footer của drawer.

- [ ] **Step 2: Add import**

Thêm `import { ThemeToggle } from '../layout/theme-toggle';` ở phần imports.

- [ ] **Step 3: Mount toggle**

Thêm `<ThemeToggle />` vào một section visible — ưu tiên gần footer hoặc top toolbar của drawer. Wrap với label nhỏ:

```tsx
<div className="flex items-center justify-between px-4 py-3 border-t border-border">
  <span className="text-sm text-muted-foreground">Giao diện</span>
  <ThemeToggle />
</div>
```

(Vị trí cụ thể tùy structure hiện tại — chèn nơi hợp lý với layout drawer.)

- [ ] **Step 4: Verify on mobile viewport**

Run: `pnpm --filter web dev`. Mở DevTools, set device emulator mobile. Mở drawer (hamburger menu). 
Expected: thấy ThemeToggle, click hoạt động.

Stop dev server.

- [ ] **Step 5: Commit**

```bash
git add apps/web/components/ui/mobile-drawer.tsx
git commit -m "feat(web): mount ThemeToggle in mobile drawer"
```

---

## Phase 2: Migrate Shared Chrome

### Task 7: Migrate header.tsx colors

**Files:**
- Modify: `apps/web/components/layout/header.tsx`

Áp dụng mapping rule (xem spec section 3.4). File này chứa header với border, bg trắng, text stone, hover stone — refactor toàn bộ sang tokens.

- [ ] **Step 1: Read current file**

Run: `cat apps/web/components/layout/header.tsx`

- [ ] **Step 2: Apply replacements**

Trong `apps/web/components/layout/header.tsx`, thay tất cả occurrences theo mapping:

| Old class | New class |
|---|---|
| `border-stone-200` | `border-border` |
| `bg-white/80` (header bg) | `bg-card/80` |
| `bg-white` (popover/dropdown bg) | `bg-popover` |
| `text-stone-900` | `text-foreground` |
| `text-stone-800` | `text-foreground` |
| `text-stone-700` | `text-muted-foreground` |
| `text-stone-500` | `text-muted-foreground` |
| `text-stone-400` | `text-muted-foreground` |
| `hover:bg-stone-100` | `hover:bg-muted` |
| `hover:bg-stone-50` | `hover:bg-muted` |
| `hover:border-stone-200` | `hover:border-border` |

Giữ nguyên `bg-emerald-700` ở logo (brand color, không phải neutral) — chấp nhận cùng màu cả 2 mode.

- [ ] **Step 3: Verify dev server**

Run: `pnpm --filter web dev` → vào trang authed (sau login). Toggle dark mode. 
Expected: header background đổi dark, text contrast OK, dropdown popover dark.

Stop dev server.

- [ ] **Step 4: Commit**

```bash
git add apps/web/components/layout/header.tsx
git commit -m "refactor(web): migrate header.tsx to semantic color tokens"
```

---

### Task 8: Migrate sidebar.tsx colors

**Files:**
- Modify: `apps/web/components/layout/sidebar.tsx`

- [ ] **Step 1: Read current file**

Run: `cat apps/web/components/layout/sidebar.tsx`

- [ ] **Step 2: Apply mapping** (xem mapping table chi tiết bên dưới)

Apply same mapping rule trong file:

| Old → New |
|---|
| `bg-white` / `bg-stone-50` (sidebar bg) → `bg-card` hoặc `bg-background` (tùy intent: sidebar surface dùng `bg-card`) |
| `border-stone-200`, `border-gray-200` → `border-border` |
| `text-stone-900`, `text-gray-900` → `text-foreground` |
| `text-stone-700`, `text-stone-600`, `text-gray-700`, `text-gray-600`, `text-gray-500` → `text-muted-foreground` |
| `hover:bg-stone-100`, `hover:bg-gray-100` → `hover:bg-muted` |
| Active nav item bg (vd `bg-stone-100`, `bg-blue-50`) → `bg-accent text-accent-foreground` |
| Brand accent (nếu có `bg-blue-*`, `text-blue-*`) → `bg-primary text-primary-foreground` cho button-like, `text-primary` cho text accent |

- [ ] **Step 3: Verify**

Run: `pnpm --filter web dev` → toggle dark → sidebar bg/text/active state đọc OK.
Stop dev server.

- [ ] **Step 4: Commit**

```bash
git add apps/web/components/layout/sidebar.tsx
git commit -m "refactor(web): migrate sidebar.tsx to semantic color tokens"
```

---

### Task 9: Migrate mobile-drawer.tsx + components/ui/index.tsx colors

**Files:**
- Modify: `apps/web/components/ui/mobile-drawer.tsx`
- Modify: `apps/web/components/ui/index.tsx`

- [ ] **Step 1: Migrate mobile-drawer.tsx**

Run: `cat apps/web/components/ui/mobile-drawer.tsx`
Apply mapping rule (same as Task 7-8).

- [ ] **Step 2: Migrate components/ui/index.tsx**

Run: `cat apps/web/components/ui/index.tsx`
Apply mapping rule. Chú ý buttons/inputs primitives — đảm bảo:
- input `border-stone-300` → `border-input`
- input `bg-white` → `bg-background`
- primary button → `bg-primary text-primary-foreground hover:bg-primary/90`
- focus ring `ring-blue-*` → `ring-ring`

- [ ] **Step 3: Verify**

Run: `pnpm --filter web dev` → open drawer trên mobile viewport, toggle dark.
Expected: drawer surface dark, inputs visible, buttons OK.

Stop dev server.

- [ ] **Step 4: Commit**

```bash
git add apps/web/components/ui/mobile-drawer.tsx apps/web/components/ui/index.tsx
git commit -m "refactor(web): migrate ui primitives + mobile drawer to semantic tokens"
```

---

### Task 10: Migrate (authed)/layout.tsx

**Files:**
- Modify: `apps/web/app/(authed)/layout.tsx`

- [ ] **Step 1: Read file**

Run: `cat 'apps/web/app/(authed)/layout.tsx'`

- [ ] **Step 2: Apply mapping**

Same mapping rule. Page wrapper bg → `bg-background`. Bất kỳ panel surface → `bg-card`. Borders → `border-border`. Text → `text-foreground` / `text-muted-foreground`.

- [ ] **Step 3: Verify**

Run: `pnpm --filter web dev` → login → toggle dark. Page background, sidebar+header+content area đều dark cohesive.

Stop dev server.

- [ ] **Step 4: Commit**

```bash
git add "apps/web/app/(authed)/layout.tsx"
git commit -m "refactor(web): migrate authed root layout to semantic tokens"
```

---

## Phase 3: Migrate Auth Pages

### Task 11: Migrate login + register pages

**Files:**
- Modify: `apps/web/app/login/page.tsx`
- Modify: `apps/web/app/register/page.tsx`

- [ ] **Step 1: Read login page**

Run: `cat apps/web/app/login/page.tsx`

- [ ] **Step 2: Apply mapping to login**

Apply full mapping rule. Lưu ý:
- Auth pages có brand panel (recent redesign) — giữ accent color như emerald/brand cho brand panel, nhưng card form area phải dùng `bg-card`, inputs `bg-background border-input`, etc.
- Error message `bg-red-50 text-red-700` → `bg-destructive/10 text-destructive`.
- Submit button → `bg-primary text-primary-foreground hover:bg-primary/90`.

- [ ] **Step 3: Verify login dark mode**

Run: `pnpm --filter web dev` → http://localhost:3000/login → toggle dark.
Expected: form readable, brand panel vẫn nhận diện được, inputs visible.

Stop dev server.

- [ ] **Step 4: Apply same to register page**

Run: `cat apps/web/app/register/page.tsx`
Apply mapping. 

- [ ] **Step 5: Verify register**

Run: `pnpm --filter web dev` → http://localhost:3000/register → toggle dark → check.
Stop dev server.

- [ ] **Step 6: Commit**

```bash
git add apps/web/app/login/page.tsx apps/web/app/register/page.tsx
git commit -m "refactor(web): migrate login + register pages to semantic tokens"
```

---

### Task 12: Migrate forgot/reset password + invite pages

**Files:**
- Modify: `apps/web/app/forgot-password/page.tsx`
- Modify: `apps/web/app/reset-password/page.tsx`
- Modify: `apps/web/app/invite/[token]/page.tsx`

- [ ] **Step 1: Migrate forgot-password**

Run: `cat apps/web/app/forgot-password/page.tsx`
Apply mapping rule.

- [ ] **Step 2: Migrate reset-password**

Run: `cat apps/web/app/reset-password/page.tsx`
Apply mapping rule.

- [ ] **Step 3: Migrate invite/[token]**

Run: `cat 'apps/web/app/invite/[token]/page.tsx'`
Apply mapping rule.

- [ ] **Step 4: Verify**

Run: `pnpm --filter web dev` → mở từng route (`/forgot-password`, `/reset-password?token=test`, `/invite/test`) → toggle dark → mỗi page readable.

Stop dev server.

- [ ] **Step 5: Commit**

```bash
git add apps/web/app/forgot-password/page.tsx apps/web/app/reset-password/page.tsx "apps/web/app/invite/[token]/page.tsx"
git commit -m "refactor(web): migrate password recovery + invite pages to semantic tokens"
```

---

## Phase 4: Migrate Authed Pages — Simple

### Task 13: Migrate settings + finance-settings

**Files:**
- Modify: `apps/web/app/(authed)/settings/page.tsx`
- Modify: `apps/web/app/(authed)/finance-settings/page.tsx`

- [ ] **Step 1: Migrate settings**

Run: `cat 'apps/web/app/(authed)/settings/page.tsx'`
Apply mapping rule. Form sections → `bg-card`, dividers → `border-border`, labels → `text-foreground`, helper text → `text-muted-foreground`.

- [ ] **Step 2: Migrate finance-settings**

Run: `cat 'apps/web/app/(authed)/finance-settings/page.tsx'`
Apply mapping rule.

- [ ] **Step 3: Verify**

Run: `pnpm --filter web dev` → login → `/settings`, `/finance-settings` → toggle dark.

Stop dev server.

- [ ] **Step 4: Commit**

```bash
git add "apps/web/app/(authed)/settings/page.tsx" "apps/web/app/(authed)/finance-settings/page.tsx"
git commit -m "refactor(web): migrate settings pages to semantic tokens"
```

---

### Task 14: Migrate family pages

**Files:**
- Modify: `apps/web/app/(authed)/family/setup/page.tsx`
- Modify: `apps/web/app/(authed)/family/invite/page.tsx`

- [ ] **Step 1: Migrate family/setup**

Run: `cat 'apps/web/app/(authed)/family/setup/page.tsx'`
Apply mapping rule.

- [ ] **Step 2: Migrate family/invite**

Run: `cat 'apps/web/app/(authed)/family/invite/page.tsx'`
Apply mapping rule.

- [ ] **Step 3: Verify**

Run: `pnpm --filter web dev` → `/family/setup`, `/family/invite` → toggle dark.

Stop dev server.

- [ ] **Step 4: Commit**

```bash
git add "apps/web/app/(authed)/family/setup/page.tsx" "apps/web/app/(authed)/family/invite/page.tsx"
git commit -m "refactor(web): migrate family pages to semantic tokens"
```

---

### Task 15: Migrate important-dates pages

**Files:**
- Modify: `apps/web/app/(authed)/important-dates/page.tsx`
- Modify: `apps/web/app/(authed)/important-dates/year/page.tsx`
- Modify: `apps/web/app/(authed)/important-dates/calendar/page.tsx`

- [ ] **Step 1: Migrate /important-dates**

Run: `cat 'apps/web/app/(authed)/important-dates/page.tsx'`
Apply mapping rule.

- [ ] **Step 2: Migrate /important-dates/year**

Run: `cat 'apps/web/app/(authed)/important-dates/year/page.tsx'`
Apply mapping rule.

- [ ] **Step 3: Migrate /important-dates/calendar**

Run: `cat 'apps/web/app/(authed)/important-dates/calendar/page.tsx'`
Apply mapping rule. Calendar grid cells: empty cell `bg-card`, hover `bg-muted`, today `border-primary`, event chip → tùy semantic (event color giữ brand hoặc map qua `bg-primary/15 text-primary`).

- [ ] **Step 4: Verify**

Run: `pnpm --filter web dev` → 3 routes → toggle dark.

Stop dev server.

- [ ] **Step 5: Commit**

```bash
git add "apps/web/app/(authed)/important-dates/"
git commit -m "refactor(web): migrate important-dates pages to semantic tokens"
```

---

## Phase 5: Migrate Authed Pages — Complex

### Task 16: Migrate transactions page

**Files:**
- Modify: `apps/web/app/(authed)/transactions/page.tsx`

- [ ] **Step 1: Read file**

Run: `cat 'apps/web/app/(authed)/transactions/page.tsx'`

- [ ] **Step 2: Apply mapping**

Apply mapping rule. Lưu ý transactions có:
- Table rows: alternate hoặc hover → `hover:bg-muted/50`
- Amount text (positive/negative) — semantic: income (green) / expense (red). Map: 
  - Income amount: giữ `text-emerald-600 dark:text-emerald-400` (brand semantic, không nằm trong token chung)
  - Expense amount: giữ `text-rose-600 dark:text-rose-400` hoặc `text-destructive`
- Filter chips: `bg-stone-100` → `bg-muted`, active → `bg-primary text-primary-foreground`
- Empty state illustration → `text-muted-foreground`

- [ ] **Step 3: Verify**

Run: `pnpm --filter web dev` → `/transactions` → toggle dark → check row hover, amount colors, filter chips, pagination.

Stop dev server.

- [ ] **Step 4: Commit**

```bash
git add "apps/web/app/(authed)/transactions/page.tsx"
git commit -m "refactor(web): migrate transactions page to semantic tokens"
```

---

### Task 17: Migrate goals page

**Files:**
- Modify: `apps/web/app/(authed)/goals/page.tsx`

- [ ] **Step 1: Read file**

Run: `cat 'apps/web/app/(authed)/goals/page.tsx'`

- [ ] **Step 2: Apply mapping**

Apply mapping rule. Lưu ý progress bars: 
- Track: `bg-stone-200` → `bg-muted`
- Fill: brand color → `bg-primary` hoặc giữ `bg-emerald-500` cho semantic "đạt mục tiêu" (manual decision per design intent — pick một, document trong comment).

FundCard từng được fix truncate (recent commit) — không touch logic, chỉ swap colors.

- [ ] **Step 3: Verify**

Run: `pnpm --filter web dev` → `/goals` → toggle dark → check fund cards, progress bars, goal numbers.

Stop dev server.

- [ ] **Step 4: Commit**

```bash
git add "apps/web/app/(authed)/goals/page.tsx"
git commit -m "refactor(web): migrate goals page to semantic tokens"
```

---

### Task 18: Migrate chat page + visibility tints

**Files:**
- Modify: `apps/web/app/(authed)/chat/page.tsx`

- [ ] **Step 1: Read file**

Run: `cat 'apps/web/app/(authed)/chat/page.tsx'`

- [ ] **Step 2: Identify visibility tint code**

Recent commits styled chat bg theo private/public visibility. Hiện tại có thể là inline class hoặc style. Tìm xem chỗ nào set bg per mode:

Run: `grep -n "visibility\|private\|public" apps/web/app/(authed)/chat/page.tsx`

- [ ] **Step 3: Refactor visibility tint sang CSS vars**

Thay color classes của tint thành inline style dùng vars `--chat-private-bg` và `--chat-public-bg` (already defined in globals.css Task 1):

```tsx
<div
  className="..."
  style={{
    backgroundColor:
      visibility === 'private'
        ? 'var(--chat-private-bg)'
        : 'var(--chat-public-bg)',
  }}
>
```

(Hoặc dùng `data-visibility` attribute + CSS, tùy pattern hiện có. Mục tiêu: tint tự switch khi `.dark` class trên html.)

- [ ] **Step 4: Apply mapping rule cho phần còn lại**

Apply mapping rule: chat bubble user → `bg-primary text-primary-foreground`, bubble assistant → `bg-card text-card-foreground` hoặc `bg-muted`. Borders, timestamps, etc. theo rule chung.

- [ ] **Step 5: Verify**

Run: `pnpm --filter web dev` → `/chat` → toggle dark, toggle private/public → tint thay đổi đúng cả 2 mode.

Stop dev server.

- [ ] **Step 6: Commit**

```bash
git add "apps/web/app/(authed)/chat/page.tsx"
git commit -m "refactor(web): migrate chat page + visibility tints to semantic tokens"
```

---

### Task 19: Migrate reports page + Recharts theme

**Files:**
- Modify: `apps/web/app/(authed)/reports/page.tsx`
- Create (optional): `apps/web/features/reports/chart-theme.ts`

- [ ] **Step 1: Read file**

Run: `cat 'apps/web/app/(authed)/reports/page.tsx'`
Tìm các Recharts component (BarChart, LineChart, PieChart) và props màu hardcoded (vd `fill="#8884d8"`, `stroke="#666"`).

- [ ] **Step 2: Create chart-theme helper**

Tạo `apps/web/features/reports/chart-theme.ts`:

```ts
'use client';

import { useEffect, useState } from 'react';
import { useTheme } from 'next-themes';

export type ChartTheme = {
  foreground: string;
  muted: string;
  grid: string;
  tooltipBg: string;
  tooltipBorder: string;
  series: string[];
};

function readVar(name: string): string {
  if (typeof window === 'undefined') return '';
  return getComputedStyle(document.documentElement)
    .getPropertyValue(name)
    .trim();
}

export function useChartTheme(): ChartTheme {
  const { resolvedTheme } = useTheme();
  const [theme, setTheme] = useState<ChartTheme>(() => buildTheme());

  useEffect(() => {
    setTheme(buildTheme());
  }, [resolvedTheme]);

  return theme;
}

function buildTheme(): ChartTheme {
  const fg = readVar('--foreground') || 'oklch(0.21 0.01 90)';
  const mutedFg = readVar('--muted-foreground') || 'oklch(0.45 0.01 90)';
  const border = readVar('--border') || 'oklch(0.92 0.005 90)';
  const card = readVar('--card') || 'white';
  return {
    foreground: `oklch(from ${fg} l c h)`,
    muted: `oklch(from ${mutedFg} l c h)`,
    grid: `oklch(from ${border} l c h)`,
    tooltipBg: `oklch(from ${card} l c h)`,
    tooltipBorder: `oklch(from ${border} l c h)`,
    series: [
      'oklch(0.65 0.18 250)',
      'oklch(0.65 0.15 30)',
      'oklch(0.65 0.15 140)',
      'oklch(0.65 0.18 320)',
      'oklch(0.65 0.15 80)',
    ],
  };
}
```

Nếu Recharts không accept `oklch()` strings, fallback strategy: dùng `getComputedStyle` rồi convert qua canvas — hoặc đơn giản hơn, define palette ở 2 mode trong constants:

```ts
const LIGHT_PALETTE = { foreground: '#1c1917', grid: '#e7e5e4', ... };
const DARK_PALETTE = { foreground: '#fafaf9', grid: '#44403c', ... };
```

Verify Recharts chấp nhận `oklch()` trước. Nếu không, fallback sang hex constants per mode.

- [ ] **Step 3: Apply theme tới charts**

Trong `reports/page.tsx`, replace hardcoded colors:
- `<CartesianGrid stroke="#ccc" />` → `<CartesianGrid stroke={theme.grid} />`
- `<XAxis stroke="#666" tick={{ fill: '#666' }} />` → dùng `theme.muted`
- `<Tooltip contentStyle={{ background: 'white', border: '1px solid #ddd' }} />` → `theme.tooltipBg` + `theme.tooltipBorder`
- Bar/Line series → `theme.series[i]`

Apply mapping rule cho phần wrapper (KPI cards, filter dropdowns, table) như task khác.

- [ ] **Step 4: Verify**

Run: `pnpm --filter web dev` → `/reports` → toggle dark → charts readable, tooltip dark, grid lines visible nhưng không quá đậm.

Stop dev server.

- [ ] **Step 5: Commit**

```bash
git add "apps/web/app/(authed)/reports/page.tsx" apps/web/features/reports/chart-theme.ts
git commit -m "refactor(web): migrate reports page + theme-aware Recharts"
```

---

## Phase 6: Verification & Cleanup

### Task 20: Grep audit for residual color classes

**Files:** none (audit only, fix tìm thấy)

- [ ] **Step 1: Run grep**

Run: 
```bash
grep -rn "bg-stone-\|bg-white\b\|bg-gray-\|text-gray-\|text-stone-\|text-black\|border-stone-\|border-gray-\|ring-blue-\|ring-stone-" apps/web/app apps/web/components --include="*.tsx"
```

- [ ] **Step 2: Review hits**

Acceptable hits (whitelist):
- Brand emerald logo in header (`bg-emerald-700 text-white`) — intentional
- Brand semantic colors trong transactions (income green / expense red) — intentional
- Auth pages brand accent panel — intentional
- Goal progress bar fill — intentional brand
- Bất kỳ chỗ nào dùng dark variant pair hợp lệ (`bg-X dark:bg-Y`) — acceptable nhưng prefer token

Each remaining hit phải có lý do trong comment HOẶC convert sang token. Fix bất kỳ chỗ nào không có lý do.

- [ ] **Step 3: Re-run grep verify clean**

Same command, expect chỉ còn whitelisted hits.

- [ ] **Step 4: Commit fixes (nếu có)**

```bash
git add -A
git commit -m "refactor(web): clean up residual raw color classes"
```

---

### Task 21: Manual QA full pass

**Files:** none (testing only)

- [ ] **Step 1: Start dev server**

Run: `pnpm --filter web dev` → http://localhost:3000

- [ ] **Step 2: Auth pages QA (light mode)**

Verify mỗi page render giống pre-refactor (regression check):
- `/login`, `/register`, `/forgot-password`, `/reset-password?token=x`, `/invite/x`
- Check: layout, form spacing, brand panel, button states

- [ ] **Step 3: Auth pages QA (dark mode)**

Toggle dark, kiểm tra lại 5 page trên:
- Text contrast (chữ đọc được)
- Form inputs: border visible, placeholder readable, focus ring visible
- Brand panel: vẫn rõ accent
- Error states: destructive color readable

- [ ] **Step 4: Authed app QA**

Login → đi qua từng route:
- `/` (dashboard nếu có), `/chat`, `/transactions`, `/goals`, `/reports`, `/settings`, `/finance-settings`, `/family/setup`, `/family/invite`, `/important-dates`, `/important-dates/year`, `/important-dates/calendar`

Mỗi route check light + dark:
- Header/sidebar render đúng
- Page content readable
- Interactive elements (button, input, dropdown) đủ contrast
- Hover states feedback rõ

- [ ] **Step 5: System mode QA**

Set toggle = "Hệ thống". Đổi OS theme (Mac: System Settings → Appearance → Light/Dark, hoặc dùng `defaults write -g AppleInterfaceStyle Dark` rồi hard reload). App phải tự follow.

- [ ] **Step 6: Persistence QA**

- Chọn Dark mode, hard reload page → vẫn dark, không flash trắng.
- Mở 2 tab cạnh nhau, đổi mode 1 tab → tab kia tự đổi sau ~1s (next-themes sync via storage event).

- [ ] **Step 7: Chat tint QA**

`/chat` → switch visibility private ↔ public ở cả light và dark → tint đổi đúng.

- [ ] **Step 8: Recharts QA**

`/reports` → có data thì check, không có thì tạo vài transaction trước. Charts ở dark mode: axis labels, grid lines, tooltip background — tất cả readable.

- [ ] **Step 9: Mobile QA**

DevTools → device emulator → iPhone size → toggle drawer → ThemeToggle hoạt động ở drawer.

- [ ] **Step 10: Record findings**

Nếu phát hiện regression hoặc page nào contrast yếu → tạo follow-up commit fix riêng, ghi notes vào commit message.

- [ ] **Step 11: Stop dev server và final commit (nếu có fix)**

```bash
git add -A
git commit -m "fix(web): address QA findings from dark mode pass"
```

---

## Definition of Done

- [ ] Toggle 3 modes hoạt động ở header desktop + mobile drawer
- [ ] `next-themes` persist + sync giữa tabs
- [ ] Không flash-of-wrong-theme khi reload
- [ ] Tất cả ~22 file `.tsx` đã migrate sang semantic tokens (whitelist exceptions allowed)
- [ ] Mỗi page render đúng ở cả light + dark
- [ ] Recharts theme-aware ở `/reports`
- [ ] Chat visibility tint hoạt động cả 2 mode
- [ ] Manual QA checklist pass
- [ ] Commits theo phase, message tiếng Anh, conventional
